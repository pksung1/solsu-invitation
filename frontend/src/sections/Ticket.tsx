import React from 'react'

const Tickets = () => {
  return (
    <div className='absolute top(0) bottom(0) w(100vw) h(100vh) bg(#fff) hbox pack'>
      {/* TICKET */}
      <div className='flex-1'></div>
    </div>
  )
}

export default Tickets